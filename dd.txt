APPROVED: Proceed to M2.

Implement ONLY M2 entropy-coded LZ2 streams.

Requirements:
1. Keep V1 completely untouched.
2. Keep all existing M1 tests and 105,000 fuzz cases passing.
3. Do not implement M3 container, M4 classifier, M5 optimal parsing, structured modeling, C/Rust acceleration, or multiprocessing.
4. Preserve the existing M1 token format/API where practical.
5. Entropy-code the separated LZ2 streams:
   - literals
   - match lengths
   - distances
   - REP symbols
6. Use canonical Huffman first. Do not implement rANS yet.
7. Add complete round-trip tests, truncation tests, malformed-table tests, and corpus tests.
8. Benchmark:
   V1 LZ vs V2 M1 raw vs V2 M2 Huffman.
9. Report:
   - compressed size
   - ratio
   - encode time
   - decode time
   - memory
   - per-stream sizes
   - Huffman table overhead
10. Pay special attention to small files: entropy-table overhead must not make them worse unnecessarily.
11. Keep a valid fallback when entropy coding is not beneficial.
12. Do not claim superiority over V1 until measurements prove it.
13. At the end, STOP. Do not begin M3 without explicit approval.