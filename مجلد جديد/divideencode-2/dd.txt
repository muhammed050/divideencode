# M4 — Branch 3 Hybrid Compression Upgrade

You are working on **DivideEncode**, a custom lossless compression project.

## CRITICAL CONTEXT

This is **Branch 3** of the project.

Do NOT modify V1.

Do NOT replace the current Branch 3 architecture with Branch 2.

Do NOT blindly merge the two branches.

The goal of M4 is to improve Branch 3 by preserving its new numeric strengths while selectively adopting the best general-purpose ideas from Branch 2.

### Current Branch 3 benchmark

Current important results:

```text
counters_u32.bin:
Branch 3 = 361 B
V1       = 9437 B
Branch 2 = 6198 B

sensor_i16.bin:
Branch 3 = 76148 B
V1       = 70900 B
Branch 2 = 88571 B

random_prng.bin:
Branch 3 = 384 B
Branch 2 = 384 B
V1       = 818 B
```

Branch 3 currently remains somewhat worse than Branch 2 on most source-code/text/structured files.

Examples:

```text
App.java:
Branch 2 = 4470 B
Branch 3 = 4887 B

main.py:
Branch 2 = 6106 B
Branch 3 = 6508 B

program.c:
Branch 2 = 4463 B
Branch 3 = 4806 B

engine.cpp:
Branch 2 = 4301 B
Branch 3 = 4734 B

data.json:
Branch 2 = 65369 B
Branch 3 = 70218 B
```

Therefore M4 must improve **general-purpose compression without sacrificing the numeric wins**.

---

# M4 OBJECTIVES

Implement M4 in the following order:

1. Preserve current Branch 3 behavior.
2. Add stronger numeric specialization.
3. Add cheap candidate probing inspired by Branch 2.
4. Improve general text/code compression.
5. Preserve the 384 B result for `random_prng.bin`.
6. Improve `sensor_i16.bin`.
7. Keep `counters_u32.bin` near its current result or better.
8. Avoid expensive exhaustive search.
9. Keep decompression deterministic and strictly validated.
10. Benchmark every change against the frozen M3 baseline.

---

# 1. FREEZE THE BASELINE FIRST

Before modifying code:

Run the complete Branch 3 test suite.

Run the complete 25-file benchmark.

Save:

```text
benchmarks/results/m4_baseline.txt
```

or an equivalent immutable baseline.

Record:

* file
* original size
* compressed size
* compression ratio
* compression time
* decompression time
* CPU
* memory
* selected mode

Verify the baseline contains the current:

```text
counters_u32.bin = 361 B
sensor_i16.bin   = 76148 B
random_prng.bin  = 384 B
```

If these values differ, STOP and investigate before implementing M4.

---

# 2. DO NOT BREAK V1

V1 is completely frozen.

Rules:

* Do not modify V1 source files.
* Do not change V1 tests.
* Do not change V1 behavior.
* V1 is only a comparison baseline.

Run the full V1 test suite after every major milestone.

Expected:

```text
V1 tests = 59/59 PASS
```

---

# 3. IMPROVE FEATURE ANALYSIS

Inspect the existing:

```text
divideencode/de2/features.py
```

Do not duplicate expensive scans.

Extend the existing single-pass FeatureSet where useful.

Consider adding cheap features such as:

```text
byte_delta_score
word_delta_score
plane_entropy
stride-2 correlation
stride-4 correlation
zero-byte density
high-byte/low-byte correlation
signed monotonicity
u16/u32 variance
small-delta frequency
repeat-distance statistics
```

Do not add expensive full-data analyses.

Prefer sampled or single-pass statistics.

The classifier must remain deterministic.

---

# 4. NUMERIC BRANCH

Improve the numeric branch while preserving the current excellent result:

```text
counters_u32.bin = 361 B
```

Investigate:

```text
byte-plane split
word-plane split
low-byte/high-byte separation
stride-2 plane split
stride-4 plane split
delta before plane split
plane split before delta
zigzag + delta
```

Use candidate generation rather than blindly applying every transform.

Example conceptual candidates:

```text
RAW
RLE
DELTA8 + LZ
DELTA16 + LZ
DELTA32 + LZ
PLANE2 + LZ
PLANE4 + LZ
DELTA32 + PLANE4 + LZ
PLANE4 + DELTA + LZ
```

The actual candidate set must be selected cheaply from FeatureSet.

Do NOT perform exhaustive search over every possible transform combination.

---

# 5. SENSOR DATA

Target:

```text
sensor_i16.bin
```

Current:

```text
Branch 3 = 76148 B
V1       = 70900 B
Branch 2 = 88571 B
```

The first M4 objective is to get Branch 3 below:

```text
70900 B
```

The long-term target is substantially lower.

Investigate:

```text
signed i16 interpretation
unsigned i16 interpretation
byte-plane separation
delta16
delta16 + zigzag
delta16 + byte-plane
byte-plane + delta
stride correlation
```

Do not assume the input is always aligned or always numeric.

The transform must only be selected when the classifier indicates that it is appropriate.

---

# 6. RANDOM PRNG REGRESSION PROTECTION

This is mandatory.

Current:

```text
random_prng.bin = 384 B
```

M4 MUST NOT regress this result.

Create an explicit regression assertion or benchmark guard:

```text
random_prng.bin <= 384 B
```

If a new optimization causes:

```text
random_prng.bin > 384 B
```

investigate before continuing.

Do not sacrifice this result for improvements elsewhere unless there is a documented architectural reason.

---

# 7. GENERAL TEXT/CODE PATH

Branch 2 is currently better on source code and text.

Investigate why.

Do NOT copy Branch 2 blindly.

Analyze its useful concepts and reproduce only the algorithmically valuable pieces.

Focus on:

```text
App.java
main.py
program.c
engine.cpp
app.js
app.ts
lib.rs
notes.md
text_en.txt
```

Target:

```text
Branch 3 <= Branch 2
```

for as many of these files as reasonably possible.

Investigate:

* preprocessing candidate ordering
* match candidate quality
* lazy parsing
* literal stream coding
* match-length coding
* distance coding
* Huffman table selection
* block size
* candidate rejection thresholds
* LZ window behavior
* rep offset behavior

Do not make the encoder exponentially more expensive.

---

# 8. STRUCTURED DATA

Target:

```text
data.json
dump.sql
table.csv
feed.xml
page.html
server.log
```

Current Branch 3 is behind Branch 2 on many of these.

Investigate whether:

```text
STRUCT
DELTA
RLE
LZ
```

should be combined selectively.

Do not force STRUCT preprocessing onto every structured file.

Use FeatureSet to decide.

Candidate selection should measure only a small shortlist.

---

# 9. CANDIDATE SELECTION

The desired architecture is:

```text
Input
  |
  v
Single-pass FeatureSet
  |
  v
Cheap deterministic classifier
  |
  v
Small candidate shortlist
  |
  +---- RAW
  +---- RLE
  +---- LZ
  +---- DELTA+LZ
  +---- STRUCT+LZ
  +---- NUMERIC/PLANE candidate
  |
  v
Encode shortlisted candidates
  |
  v
Measure actual encoded size
  |
  v
Choose smallest valid result
```

Do not run every transform combination.

Do not restore the old exhaustive DE1 search.

---

# 10. SPEED REQUIREMENT

Compression ratio is not the only metric.

For every optimization measure:

```text
compressed size
encode time
decode time
CPU
memory
```

A candidate that saves a few bytes but costs several times more CPU should normally be rejected unless the size gain is substantial.

Especially monitor:

```text
sensor_i16.bin
counters_u32.bin
```

because the current Branch 3 numeric improvements already show increased encode cost.

The goal is a better Pareto point:

```text
smaller
+
fast enough
+
simple decode
```

---

# 11. CONTAINER COMPATIBILITY

Do not unnecessarily change the DE2 container format.

Existing:

```text
DE2
version
flags
varints
CRC
per-block metadata
raw length
compressed length
payload CRC
transform metadata
```

If a new transform requires metadata, add it through a clean versioned `tmeta` representation.

Decoder requirements:

* strict lengths
* strict mode validation
* strict transform validation
* CRC verification
* trailing-byte rejection
* truncation detection
* corruption detection

Never silently accept malformed data.

---

# 12. TESTING

Add tests for every new transform.

Required categories:

```text
roundtrip
empty input
1 byte
small inputs
unaligned lengths
all-zero data
constant data
monotonic numeric data
random numeric data
high entropy data
corruption
truncation
invalid metadata
invalid mode
trailing bytes
```

Add fuzz tests for the new numeric transforms.

Minimum requirement:

```text
existing tests PASS
+
new M4 tests PASS
```

Target:

```text
0 regressions
```

---

# 13. BENCHMARK REQUIREMENT

After implementation, run the full 25-file benchmark.

Compare:

```text
V1
Branch 2 frozen benchmark
Branch 3 M3 baseline
Branch 3 M4
zlib
gzip
brotli
zstd
xz
```

Generate:

```text
benchmarks/results/m4_results.csv
benchmarks/results/m4_results.md
```

The Markdown report must include:

### Overall

```text
original total
compressed total
ratio
total encode time
total decode time
```

### Per file

```text
file
original
V1
Branch2
Branch3-M3
Branch3-M4
best
encode time
decode time
mode
```

### Win counts

```text
Branch 2 wins
Branch 3 M3 wins
Branch 3 M4 wins
ties
```

---

# 14. M4 ACCEPTANCE CRITERIA

M4 is NOT complete unless:

### Correctness

```text
V1 = 59/59 PASS
DE2 existing tests = 100% PASS
M4 tests = 100% PASS
```

### Regression

```text
random_prng.bin <= 384 B
counters_u32.bin <= 361 B
```

Preferably improve both.

### Numeric

```text
sensor_i16.bin < 70900 B
```

This is the minimum target.

### General compression

Branch 3 M4 should beat Branch 3 M3 on the majority of compressible files.

### Overall

M4 should improve the Branch 3 corpus ratio over:

```text
0.3530
```

and should move toward or below Branch 2's:

```text
0.3391
```

### Speed

Do not introduce a return to DE1-style multi-second exhaustive encoding.

The classifier + shortlist architecture must remain efficient.

---

# 15. IMPORTANT ENGINEERING RULE

Do not optimize based on one file.

Every optimization must be evaluated against the entire 25-file corpus.

A change such as:

```text
counters_u32: 361 B → 200 B
```

is NOT automatically good if it causes:

```text
App.java: 4887 → 7000
sensor_i16: 76148 → 150000
random_prng: 384 → 5000
```

Optimize for the whole codec.

---

# 16. IMPLEMENTATION PROCESS

Work incrementally.

### Phase A

Baseline + feature improvements.

### Phase B

Numeric/plane candidates.

### Phase C

Sensor optimization.

### Phase D

General LZ/text optimization.

### Phase E

Candidate selection tuning.

### Phase F

Full benchmark + regression testing.

After every phase:

```text
run tests
run targeted benchmark
compare against M4 baseline
```

Do not continue if a major regression appears.

---

# 17. FINAL REPORT

When M4 is complete, report:

```text
M4 STATUS

Tests:
V1:
DE2:
M4:

Corpus:
Original:
Branch 2:
Branch 3 M3:
Branch 3 M4:

Ratio:
Encode time:
Decode time:

Wins:
Branch 2:
Branch 3 M3:
Branch 3 M4:

Key files:
counters_u32:
sensor_i16:
random_prng:
data.json:
App.java:
main.py:

New transforms:

New classifier features:

Files changed:

Regressions:

Remaining gaps:

Recommendation for M5:
```

Do not claim success without measured benchmark results.

Do not fabricate numbers.

Do not modify V1.

Do not delete Branch 3 M3 baseline.

Do not stop after implementing code without running the full tests and benchmark.

## FINAL GOAL

Build **Branch 3 M4 as a hybrid adaptive codec** that combines:

```text
Branch 2's strong general compression
+
Branch 3's numeric specialization
+
Branch 3's FeatureSet/classifier
+
fast candidate selection
+
strict DE2 container
+
fast decoding
```

The objective is not merely to beat one benchmark.

The objective is to create the strongest, fastest, most balanced version of **DE2 V2** so far.

**Start with baseline verification, then implement M4 incrementally.**
